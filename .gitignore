renderer/node_modules/
*.log
nvim.log
.DS_Store
tmp/
.claude/
# Local Obsidian workspace state -- this repo doubles as a test vault
# (it has a .git marker), and that state is per-machine.
.obsidian/
# Agent instructions -- local working file, not part of the public plugin
CLAUDE.md
# Editor workspace settings and local release helpers -- per-machine
.vscode/
